"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useScreenOrientationLock = useScreenOrientationLock;

var _react = require("react");

var ScreenOrientation = _interopRequireWildcard(require("expo-screen-orientation"));

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function _getRequireWildcardCache() { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

/**
 * Get or lock the screen orientation of the device.
 * This hook will always fetch the current device lock.
 * It will also apply the device lock when this is provided.
 *
 * @see https://docs.expo.io/versions/latest/sdk/screen-orientation/
 * @example const [lockInfo, lockError] = useScreenOrientationLock(...);
 */
function useScreenOrientationLock(lock) {
  const [lockInfo, setLockInfo] = (0, _react.useState)();
  const [lockError, setLockError] = (0, _react.useState)();
  (0, _react.useEffect)(() => {
    ScreenOrientation.getOrientationLockAsync().then(setLockInfo);

    if (lock) {
      ScreenOrientation.lockAsync(lock).then(() => setLockInfo(lock)).catch(setLockError);
      return () => {
        ScreenOrientation.unlockAsync();
      };
    }
  }, [lock]);
  return [lockInfo, lockError];
}
//# sourceMappingURL=use-screen-orientation-lock.js.map